const myNums = [700,800,1500]
let newNum = [200,300]

// myNums.map( (num) => console.log(num+10) )

/* map value can be store */

let newNums = myNums.map( (num) => (num+10) )

// console.log(newNums);