//Control Flow
//You will not every time run entire code.

//Bitwise Operators
// And = && (empathises)
//Or = || (Pipesign)

const islogged = true
if (islogged = true) {
    console.log("user is logged");
} else {
    console.log("user not logged in")
}